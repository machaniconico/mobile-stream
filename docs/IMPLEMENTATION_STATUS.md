# Implementation Status

## Implemented Now

- TypeScript app scaffold with Vite.
- OBS-like multi-scene/source/render graph domain model with Main, Starting Soon, Break, and Privacy Shield presets, active scene switching, and cut/fade transition settings.
- Persisted scene collection normalization with transition settings, clamped canvas/source/transform/avatar runtime values, and legacy single-scene migration.
- Text overlay presets for subtitles, lower thirds, tickers, notices, badges, labels, and live captions, including categorized quick subtitle/text decks for live operation, automatic visible live-caption text source creation when live captions are enabled, automatic live-caption source hiding when captions are disabled, and retained native text/caption/chat applied-kind proof for commercial release evidence.
- Stream profile model for RTMP and RTMPS destinations, including YouTube Live, Twitch Auto, Twitch Tokyo, and Custom presets.
- Commercial-start readiness checks for endpoint, protocol, stream key, quality, and scene safety.
- Mobile scene complexity readiness checks warn before public launch when total sources, visible layers, native overlays, text overlays, or duplicate chat overlays exceed conservative phone-safe budgets without retained physical-device evidence for that exact composition.
- RTMP/RTMPS destination normalization for pasted YouTube/Twitch full publish URLs across shared profile handling and native endpoint assembly.
- Shared Go Live preflight model for launch blocking, warning surfacing, engine-state duplicate-start protection, and failed-start operation logging.
- Go Live platform-state preflight for platform-visible YouTube streams, requiring a bound broadcast/stream before launch after commercial validation, blocking completed broadcasts, warning on already-live broadcasts, blocking YouTube Public and Twitch starts when dashboard status is stale or unchecked or platform publishing OAuth evidence is missing/unknown while keeping unlisted validation starts as warnings, and blocking Twitch starts when the channel status is already live.
- Platform-visible Go Live preflight blocks visible Live2D or VRM/VRoid preview-only scenes until native Live2D Cubism or VRM rendering is integrated and validated; VRM/VRoid scenes are promoted out of preview-only blocking only when retained iOS/Android native renderer/model/pose proof is present.
- Platform-visible Go Live preflight blocks native compositor drift risks, including preview-only source ordering, missing screen capture, and PNGTuber/image assets that the iOS ReplayKit Broadcast Upload Extension cannot load from App Group storage.
- Public YouTube and Twitch start preflight/checklist locks block warnings as well as failures, including stale dashboard evidence, disabled/unsafe chat readout, avatar tracking that is disabled/simulated/lost/not moving, unsafe/muted mic monitoring, enabled live captions without final cue proof, unresolved text/chat overlay transparency/readability/safe-area/avatar-overlap warnings, incomplete commercial evidence, Android RootEncoder compatibility mode for platform-visible Android starts, and mock-engine fallback.
- Public YouTube and Twitch starts require an explicit final operator confirmation after the public launch checklist passes, and accepted/cancelled confirmations are retained as safety/operation audit events with safe target and checklist evidence.
- Commercial release gates keep Go Live preflight warnings, public-launch checklist warnings, commercial validation warning/pending items, private launch rehearsal warnings, stale retained validation runs, retained-run manifest count mismatches, native publisher congestion, queued-frame backlog, publisher video/audio drops, rejected live render-graph subtitle/text updates, native compositor dropped frames, and other release warnings fail-closed, matching the app-side start lock, validation checklist, and support-bundle audit trail for platform-visible streams.
- Stream diagnostics model for redacted publish URL, endpoint parsing, upload target estimate, readiness issues, live telemetry health checks, sanitized diagnostic reports, and final JSON/share-text export redaction for supplied stream-key candidates, OAuth/API credentials, contact details, invite links, and protocol-less private links while preserving non-secret ingest host/application support context.
- Face-tracking production diagnostics for prepared PNGTuber assets, simulated/native-camera input risk, tracking runtime state, runtime frame freshness, native face-landmark confidence/readiness, active motion count, still-image rig quality score/grade, IRIAM-style face-part separation, pseudo-depth continuity, semantic-segment, and explicit eye/mouth deformation-band high-fidelity scores with Go Live warnings below ready grade, browser Inspector and React Native source-control rig-map tuning visualization, current-runtime Go Live preflight warnings, public launch start-lock evidence, support reports, and commercial validation checklist evidence.
- Native composition readiness/diagnostics model that distinguishes native-supported avatar/image/solid/text/caption/chat overlays from underlay ordering, retains native text/caption/chat overlay counts in diagnostics and support bundles, reports Live2D/VRM production gaps, iOS Broadcast Upload Extension still-image asset access risks, VRM pose-payload delivery evidence, native VRM/GLB model-header load evidence, native VRM version/humanoid-bone/expression/renderability and renderer-compatibility metadata evidence, native VRM pose-to-model bone/expression compatibility evidence, platform-accepted production VRM renderer backend/model-loaded/rendered-source evidence, and effective VRM native-composition promotion before device RTMP publishing.
- Stream health history model for average/min bitrate and FPS, dropped-frame/reconnect trend summaries, diagnostics checks, and report export.
- Post-stream session summary model for completed session outcome, warning/failure/recovery counts, health summary, lip-sync/audio-meter sample summary, spoken-chat success/failure counts, and next-stream recommendations.
- Durable completed stream session summary persistence for Web localStorage and React Native native storage, with corrupt payload recovery and retention limiting.
- Completed stream session history management in Web and React Native diagnostics, including local history clearing for privacy/support resets.
- Completed stream session history trend analysis for clean-rate, stability, average duration, recovery totals, and next-stream recommendations across diagnostics, support bundles, Web, and React Native.
- Completed stream session summaries and diagnostic reports retain safe native runtime evidence, including publisher state, compositor status, applied overlay kinds for text/caption/chat/subtitle audit, live render-graph reload/rejected-update counters for subtitle/text updates, still-image asset loaded/missing/decoded/composited counts and decoded/composited pixel totals, VRM pose payload active/missing counts, VRM model version/humanoid-bone/expression/renderability/renderer-compatibility counts, VRM pose bone/expression applied/unsupported counts, VRM renderer backend/model-loaded/rendered-source counts, congestion, queue size, sent/dropped frame counts, frame interval average/max/jitter proof, and bytes, without storing raw native error messages.
- Commercial pre-release validation checklist for configuration readiness, RTMPS transport, private ingest smoke tests, destination dashboard checks, physical-device evidence, retained session baselines, and support-bundle evidence across diagnostics, support bundles, Web, and React Native.
- Private RTMP(S) validation runbook for setup, native start, mic FX/headphone monitor checks, YouTube/Twitch chat readout checks, stable monitor hold, destination dashboard check, clean stop, and retained evidence recording across diagnostics, support bundles, Web, and React Native.
- Physical validation evidence recording for iOS/Android private RTMP(S) runs, including retained pass/warn/fail run history, stable monitor-hold bitrate/FPS/drop/reconnect proof, mic FX/headphone monitor snapshots, native self-monitor write/drop proof, device output-route/headphone safety, lip-sync/audio-meter sample summaries, native face-landmark confidence/readiness, YouTube/Twitch chat readout snapshots, spoken-chat success/failure counts, YouTube broadcast privacy proof, Web localStorage, mobile native storage, diagnostics integration, support bundle summaries, and Web/React Native recorder UI.
- Physical-device preflight CLI and release-candidate gate for release rehearsals, with `adb devices -l`, Android `getprop` qemu/hardware probes, and `xcrun xctrace list devices` checks that accept connected physical Android/iOS devices, reject Android emulators, iOS Simulators, unavailable/offline/unauthorized devices, redact device identifiers to stable hashes, avoid retaining personal iOS device names, retain sanitized host/tool evidence including `adb version` and `xcrun xctrace version`, optionally write a symlink-safe JSON preflight artifact, require that artifact for store-submission release-candidate evidence, and carry it into release reports/evidence packages.
- Physical validation evidence now records explicit physical-device identity proof, rejects Simulator/Emulator/browser/generic test-device labels, surfaces physical iOS/Android coverage in Web/React Native diagnostics, and requires support bundle v55 scene composition fingerprint, physical-device proof, current profile and retained Android validation-run direct MediaCodec publisher-mode proof, current destination/protocol/scene scope, passing private launch rehearsal, stable monitor-hold sample/duration/drop/reconnect, controlled weak-network quality automation, text overlay and live caption launch evidence, native frame/byte/frame-interval/non-congested publisher/zero publisher-drop/compositor applied/skipped proof for the current-scene native overlay count, text/caption/chat applied-kind proof plus live render-graph reload/rejected-update proof for subtitle/comment overlays, platform-accepted production video/audio encoder backend proof and loaded/decoded/composited still-image asset pixel proof, platform-accepted production VRM renderer/backend/model/pose proof when VRM sources are present, mic/headphone native monitor route-match/write/drop/latency source/budget plus Bluetooth tuning-note proof when applicable, fresh avatar-tracking runtime, ready native face-landmark confidence, retained manifest PNGTuber prepared-count/rig-quality/high-fidelity/semantic-segment/eye-mouth-segment proof or VRM visible-count/native-renderer proof, spoken chat-readout, YouTube/Twitch dashboard identity/state manifest proof including Twitch title/category/language metadata, and same-run platform ingest proof with retained checked-at and observed-age consistency before commercial release approval.
- Physical validation run and evidence summaries include stable fingerprints, retained-run manifests, and Android direct MediaCodec publisher-mode readiness across diagnostics reports, Web/React Native panels, and support bundles so release-candidate audit/support records can be correlated and accidental evidence mixups, Android fallback publisher usage, or out-of-scope retained runs are easier to spot.
- Release-candidate validation evidence gating for freshness, current destination/protocol/scene matching, same-build iOS/Android coverage, stable monitor-hold proof, native publisher/compositor proof, fresh YouTube/Twitch dashboard checked-at proof, stale evidence blockers, and support-bundle eligible/stale run counts.
- Launch rehearsal readiness report across diagnostics, support bundles, Web, and React Native, blocking commercial release unless the rehearsed private ingest, runbook, feature proof, and retained release evidence are ready to promote.
- Pre-launch rehearsal scoring grades target security, private ingest, runbook completion, feature proof, and retained release evidence on a 100-point scale across diagnostics, support bundles, Web, React Native, and commercial release bundle verification.
- Commercial release gate for saved support bundle JSON, with machine-readable pass/warn/fail checks for bundle freshness/schema, public launch lock state, runbook completion, same-build iOS/Android evidence, retained-run manifests, manifest/summary integrity, and feature proof before release approval.
- Commercial release support-bundle privacy gate that fails release approval when exported evidence still contains unredacted OAuth tokens, stream keys, client secrets, device codes, Authorization headers, email addresses, phone numbers, invite links, or protocol-less private links.
- Browser UI verification writes JSON evidence with desktop/mobile screenshot hashes; release-candidate UI skips now fail unless that same-commit evidence is supplied.
- Local release-candidate verification command that fails closed on uncommitted source changes, requires store-release orchestration report and physical-device preflight evidence when store-submission checklist evidence is present, revalidates distribution/dashboard/final store-submission handoff manifests and physical-device preflight artifacts against the current commit, requires browser UI evidence to come from a loopback preview target with git commit/dirty-state provenance, fails fast on the commercial support-bundle gate before expensive source gates, then chains repository automation checks, native release-config checks, unit tests, web/RN typechecks, web build, web bundle-size verification, browser UI verification, React Native iOS/Android bundles, Android native debug APK build, and iOS native simulator app build, with JSON audit evidence for git revision, dirty state, support-bundle SHA-256, generated web/RN/Android/iOS/UI/physical-device preflight artifact hashes, release-configuration input hashes, production Android/iOS native source hashes, gate outcomes, timings, and failure reasons.
- Saved release-candidate report audit that revalidates archived RC reports against the current commit, clean git state, absence of warning approval, required report/UI-evidence git provenance, support-bundle SHA-256 plus the support bundle's commercial release gate at the report timestamp, loopback browser UI evidence schema/freshness/text checks, generated artifact hashes, release-configuration/native-source input hashes, and required gate outcomes before commercial approval.
- Store-submission real-device screenshot import flow that upgrades UI draft store metadata to final App Store / Play Console screenshot evidence, preserves listing metadata, requires canonical workspace-relative metadata/review/screenshot record paths, records real device OS/app-build/captured-at proof, rewrites source labels to `realDevice`, rejects Simulator/Emulator/browser/test-device labels in final screenshot gating, regenerates review/checklist artifacts, and runs final screenshot gating before approval.
- Store-submission approval now cross-checks real-device screenshot app-build labels against the support bundle's consistent physical-validation build, blocking final App Store / Play Console approval when listing screenshots come from a different build than the retained release-candidate evidence.
- Store-submission checklist manifests now record PNG screenshot dimensions, and final store-submission gating blocks placeholder/tiny screenshots before App Store / Play Console approval evidence can pass.
- Dashboard, store-submission, and browser UI evidence screenshot gates reject structurally invalid PNG files, including forged IHDR dimensions with stale CRCs, invalid chunk layout, unsupported interlacing, and IDAT data that does not match declared dimensions.
- Release automation script syntax verification is part of CI, repository automation audit, and the local release-candidate gate, so broken `.mjs` release tooling is caught before commercial approval.
- GitHub Actions automation safety audit keeps the required commercial gate coverage in CI, requires read-only default token permissions plus Java/Android SDK provisioning for Android native builds, checks the macOS iOS native workflow, and requires an explicit `automerge` label before PR auto-merge can be enabled.
- Browser studio code-splitting keeps the shell and main studio screen in separate JavaScript chunks, with CI bundle-size verification after the web build.
- Physical validation runs retain safe native runtime evidence from the current native publisher/compositor or the latest completed session; validation only passes when iOS and Android retained evidence include sent video/audio frames, bytes written, clean compositor state, fully loaded, decoded, and composited still-image assets with non-zero pixel totals, and ready VRM renderer/model/pose proof for any retained VRM/VRoid source.
- Physical validation native-runtime evidence is cross-checked against the current scene's native composition requirements, so screen-only or under-counted runtime telemetry cannot approve a scene that needs PNGTuber/image overlays, and VRM/VRoid scenes require renderer/model/pose proof before preview-only composition warnings are cleared.
- Physical validation runs retain stable monitor-hold proof; validation only passes when each platform has at least 60 seconds and 3 samples of stable bitrate/FPS telemetry with zero drops and zero reconnects.
- Physical validation runs retain mic FX/headphone monitor route, native self-monitor write/drop proof, and chat readout evidence; audio validation only passes when native monitor write/drop proof is present with written frames, written buffers, zero drops, and headphone route proof.
- Physical validation runs retain measured or native-estimated mic monitor latency, latency source, Bluetooth route detection, and explicit Bluetooth route tuning-review evidence; audio validation only passes when processed-mic self-monitor latency is within the route-specific release budget and Bluetooth routes include a reviewed tuning note.
- Physical validation runs retain safe YouTube/Twitch dashboard evidence, including YouTube broadcast/stream/channel identity, YouTube broadcast/stream/health status, Twitch broadcaster/stream identity, Twitch live/offline status, screenshot capture timestamps, per-run dashboard freshness snapshots, and same-run platform ingest proof that pairs native sent-frame telemetry with destination receiving state plus checked-at/run-time and retained observed-age consistency; unhealthy, stale, timestamp-mismatched, unchecked dashboard states, reused dashboard age claims, missing observed-age proof, or missing same-run send/receive proof downgrade retained validation outcomes.
- Release-candidate checklist and private validation runbook require fresh YouTube/Twitch dashboard checked-at evidence before treating destination dashboard validation as complete.
- Live quality advisor model for maintaining, monitoring, lowering quality, reconnecting, or stopping based on active incidents, history, and recovery state, with safe suggested-quality application in Web and React Native.
- Shared quality automation guard for Web and React Native that raises live-session quality alerts, records quality automation events, and automatically applies a safer next-stream target only after the encoder is no longer live.
- Privacy Shield emergency control across Web and React Native switches to a blackout scene, mutes mic/app/chat broadcast mixer channels, disables self-monitor playback, stops chat readout, records a safety audit event, and pushes safe scene/profile updates to the native engine when live.
- Live quality update API for Mock, Android, and iOS, allowing same-resolution bitrate/FPS pressure relief while Android stays live, while iOS refreshes the App Group quality target for the next ReplayKit broadcast restart.
- Completed session summaries, physical validation evidence, diagnostics, validation panels, and support bundles retain live quality automation evidence, including live bitrate/FPS updates, next-start target changes, and failed native quality updates.
- Private validation runbook includes a weak-network quality stress fallback step, requiring retained live quality-update or next-start fallback evidence before the runbook can be complete, and blocking when native quality updates fail.
- Public launch checklist surfaces incomplete private validation runbook evidence, including missing weak-network quality stress proof, as a pre-launch commercial-evidence warning for YouTube Public and Twitch starts.
- Redacted support bundle export combining preflight, diagnostics, face-tracking runtime freshness and still-image rig high-fidelity scores, scene composition, native still-image asset load/decode pixel evidence, stream target, and safe profile summaries for support triage, with final JSON/share-text export redaction for supplied stream-key candidates, OAuth/API credentials, contact details, invite links, and protocol-less private links while preserving non-secret ingest host/application support context.
- Stream quality incident model for active bitrate/FPS/drop/reconnect problems, recommendations, UI surfacing, and report export.
- Shared stream recovery policy model for retry budget, bounded exponential backoff, degraded telemetry detection, failed-engine recovery advice, and sanitized report export.
- Shared app-level automatic recovery scheduler for Web and React Native, including failed-engine reconnect, critical telemetry hold time, retry budget enforcement, and timer deduplication.
- Web and React Native automatic recovery respects the public launch checklist for unstarted failed-stream recovery while still allowing active live/reconnecting sessions to recover without being blocked by transient dashboard/chat/audio evidence changes.
- Shared stream session event log for status transitions, manual operations, platform API started/succeeded/failed/skipped audit events, automatic recovery actions, support diagnostics, and redacted report export.
- Stream key redaction and no-secret browser persistence.
- Physical validation run and completed stream session summary persistence redact supplied stream-key candidates plus OAuth/API tokens, device codes, client secrets, Authorization headers, email addresses, phone numbers, invite links, and protocol-less links before writing Web localStorage or mobile native storage.
- Sensitive OAuth/API error message redaction before user-facing status display.
- OAuth, stream-key, platform publishing, and YouTube chat API calls fail closed with sanitized HTTP/unreadable-JSON errors when upstream responses are empty, non-JSON, or unavailable, while retaining status code, retryability, and `Retry-After` metadata for diagnostics and safe app status retry guidance.
- Native Android/iOS status and publisher-runtime message redaction for publish URLs, stream keys, and bearer tokens before app display.
- In-app stream key clear/replacement controls for browser and mobile setup screens.
- Mic effect profile model with presets, gain, noise gate, compression, and headphone monitor settings.
- Broadcast audio mixer profile with per-channel mic/app-audio/chat-readout volume and mute settings, launch/diagnostics silent-mix detection, Android mic broadcast gain/mute, and iOS ReplayKit app/mic mix gain.
- Shared audio-route safety model for speaker/receiver/wired/USB/Bluetooth/AirPlay/HDMI output state, monitor-route diagnostics, and Go Live preflight blocking when headphones-only monitoring is routed to speakers.
- Chat/comment read-aloud domain model with queueing, muted words, command-message skipping, duplicate suppression, per-viewer rate limiting, excessive-caps spam blocking, optional link blocking, queue limits, URL/protocol-less link redaction, OAuth/Authorization secret redaction, email/phone/invite contact redaction, speech text shaping, and launch preflight warnings when public-stream safety controls are disabled.
- Text overlay scene sources support OBS-style top-title, subtitle, lower-third, center-notice, ticker, corner-badge, and live-caption presets, labels, multiline subtitles, ticker-style text, timed one-shot text/subtitle display with auto-expiring render-graph output, live-operation Quick Subtitle entry, queued subtitles that wait behind the active timed text window without rendering early, persistent text pinning, manual text hiding, categorized one-tap quick text/subtitle preset decks on Web/React Native, and live-caption runtime cue overlays with one-tap preset styling, editable fallback copy, alignment, speaker-name toggle, font size, max lines, text color, translucent backdrop, outline color/width, bounded sanitized render-output lines for manual text and speaker-prefixed captions, long-token wrapping before Web/React Native/native render output, Web/React Native source controls, native iOS/Android compositor rendering for RTMP/RTMPS output, iOS ReplayKit live render-graph refresh for in-flight subtitle/text changes with retained reload/rejected-update counters, retained native applied-kind evidence for text/caption/chat subtitle/comment output, Go Live warnings and release evidence for large opaque text backdrops, clipped/unreadable text layout risks, non-ticker text safe-area edge risks, and text-over-avatar overlap risks, plus launch blocking when visible text would expose stream keys, OAuth tokens, callback secrets, API credentials, email addresses, phone numbers, or invite links.
- Live caption session state supports Web Speech Recognition, Android SpeechRecognizer, iOS SFSpeechRecognizer, interim/final caption cue handling, stale-cue expiry, language selection, test cue injection, caption length/hold controls, URL, protocol-less link, OAuth/Authorization secret, email, phone, and invite-link redaction, Privacy Shield shutdown, disabled-caption render-graph suppression, and shared Web/React Native/native-render-graph runtime delivery without persisting recognized caption text into scene storage.
- Live caption launch diagnostics and Go Live preflight now fail enabled captions with no visible Live Caption source or unsupported/error speech recognition, warn until a final caption cue is confirmed, expose caption source/cue/transcript counts in diagnostic reports, and block YouTube Public/Twitch starts through the public launch lock when caption issues remain.
- Transparent chat/comment overlay scene source with runtime-only recent message payloads, author toggle, line limits, per-source message length limits, URL/protocol-less link redaction, OAuth/Authorization secret redaction, email/phone/invite contact redaction, text color, optional semi-transparent backdrop, native/web compositor support, readiness/preflight warnings and release evidence when a visible chat backdrop is non-transparent enough to cover gameplay/avatar overlays, comments may clip or render unreadably, chat is too close to phone/platform safe-area edges, or chat substantially overlaps the avatar layer, and launch warnings/public-stream blocking when visible chat overlay URL redaction is disabled, without persisting comment text into scene storage.
- Platform chat adapter model for YouTube LiveChatMessages responses and Twitch EventSub chat notifications.
- Platform chat adapter settings persisted with the shared studio profile on web and mobile.
- OAuth chat authorization layer for YouTube PKCE authorization-code callbacks, Twitch implicit callbacks, and Twitch Device Code Flow, with tokens kept out of persisted profiles.
- Secure mobile OAuth credential persistence through Keychain/Android Keystore-backed native storage.
- YouTube/Twitch OAuth refresh-token rotation and hourly Twitch token validation scheduling for mobile chat authorization, with bounded background retry scheduling for retryable secure-storage sync failures and `Retry-After`.
- Web and React Native platform API operations use a visible shared single-flight guard to reject and UI-lock overlapping stream-key, publishing, broadcast lifecycle, OAuth exchange, and mobile secure-storage maintenance requests while showing the active operation label and retaining session audit events with safe retry guidance.
- Platform-specific OAuth credential store for retaining YouTube and Twitch credentials at the same time, with legacy single-credential mobile storage migration.
- OAuth credential health checks for retained platform, scope, and expiry evidence before chat readout, platform-visible Go Live checks, and YouTube broadcast Test/Live/Complete lifecycle controls.
- OAuth/API-backed platform stream key controls: YouTube reusable stream creation for key rotation and Twitch Helix current-key sync.
- OAuth/API-backed platform publishing controls: YouTube scheduled Live Broadcast creation, stream binding, broadcast/ingest health refresh, and test/live/complete lifecycle transitions; Twitch channel title/category/language metadata update plus live/offline status refresh.
- Platform publishing status refreshes retain YouTube/Twitch dashboard checked-at timestamps across profile storage, diagnostics, support bundles, and Web/React Native setup screens.
- Diagnostics reports and support bundles surface platform API audit counts, YouTube/Twitch dashboard status freshness, YouTube broadcast privacy mismatch checks, YouTube bound-stream mismatch checks, and Twitch dashboard title/category/language mismatch checks so release-candidate evidence can prove whether platform checks were current.
- Web and React Native diagnostics panels show current and retained dashboard freshness so stale/missing YouTube/Twitch checks are visible before recording release evidence.
- Web and React Native Go Live areas show a shared public launch checklist covering destination/scene, avatar tracking, platform dashboard freshness, chat readout, mic FX headphone monitoring, commercial evidence, and encoder state, with YouTube Public and Twitch starts locked when checklist warnings or failures remain while private/unlisted validation starts stay available.
- Web and React Native start handlers recompute the shared start preflight and public launch checklist immediately before native/mock engine prepare/start, so unsafe YouTube Public and Twitch starts are rejected even if invoked outside the disabled Go Live button path.
- Diagnostics reports and support bundles retain the shared public launch checklist, start-lock state, pass/warn/fail counts, and operator actions for release-candidate audit trails.
- Support bundles summarize retained public launch confirmation events, including the latest accepted/cancelled status, timestamp, and safe target/checklist evidence for release-candidate and incident audit.
- YouTube Public broadcast Live transitions reuse the shared public launch checklist and block public-visibility failures such as chat/audio/evidence readiness while ignoring the expected already-live local encoder item.
- YouTube Live broadcast lifecycle transition preflight for Test/Live/Complete actions, including public-validation gating, local encoder state checks, active ingest checks, fresh dashboard-status checks, health checks, and local-stream-stop protection before completing a broadcast across Web and React Native.
- OAuth-token chat connection layer for YouTube live chat polling and Twitch IRC WebSocket ingestion.
- Platform API controls refresh expiring YouTube/Twitch OAuth credentials before stream-key, broadcast, and channel operations on Web and React Native.
- Mobile OAuth callback URL scheme registration and React Native callback capture for chat authorization.
- Avatar runtime model for PNGTuber/Live2D/VRM expression, lip sync, and auto blink.
- Live2D source records now persist Cubism `model3.json` URIs, expose them in Web/React Native inspectors, warn on missing/remote/relative/unsupported model-package URIs, and include a shared `model3.json` manifest validator for Cubism version compatibility, Moc, texture image format readiness, expression/motion file coverage, physics/pose/reference extension checks, duplicate packaged references, and unsafe package references.
- VRM/VRoid source records now persist VRM/GLB model URIs, expose them in Web/React Native inspectors, can pick/prepare local `.vrm`/`.glb` files into native-readable storage, warn on missing/remote/relative/unsupported or non-model URIs, and include shared plus native binary GLB validators that detect VRM 1.0/0.x extensions, unsupported required glTF extensions, self-contained BIN buffer readiness, external buffer/image URI risks, humanoid bone mappings, expression/blendshape metadata, mesh primitives, triangle primitive compatibility, unsupported primitive modes, skinned mesh primitives, POSITION/NORMAL/TEXCOORD_0 accessors, vertex/index counts, accessor bounds, JOINTS_0/WEIGHTS_0 skinning attributes, skin joints, morph targets, materials, transparent materials, textures, image counts, unsupported image MIME/URI formats, mobile model-size/mesh/material/texture budget warnings, native runtime load evidence, renderability counts, renderer-compatibility counts, geometry counts, and pose-to-model compatibility counts.
- VRM/VRoid runtime pose mapping converts shared avatar motion into native-consumable humanoid bone rotations, expression/blendshape weights, look-at values, root offsets, render-graph payloads, native runtime pose-delivery telemetry, pose-to-model compatibility telemetry, renderability telemetry, renderer readiness evidence, validation evidence, public preflight promotion evidence, and support-bundle pose summaries.
- Still-image VTuber face tracking runtime with native-camera frames on mobile, simulated frames on web, neutral calibration, dead-zone filtering, jump limiting, lost-face return tuning, image-aspect-aware geometry rig inference, browser alpha-mask foreground analysis for transparent/padded PNG auto-rigging, browser dark-pixel eye/mouth feature extraction for single-illustration auto-rigging, native iOS/Android still-image alpha/dark-pixel eye-mouth analysis with image-orientation normalization for React Native auto-rigging, browser FaceDetector-to-semantic-landmark auto-rig handoff when available, iOS Vision / Android ML Kit semantic landmark telemetry, detector/native ML landmark adapter contract, production rig-line/face-range/headroom/face-center diagnostics, IRIAM-style face-part separation, pseudo-depth continuity, and semantic face/eye/mouth/body segment scoring, retained native face-landmark release evidence, support-bundle rig quality/high-fidelity/semantic-segment/eye-mouth score evidence, iOS/Android native compositor 2.5D motion scaling, tunable illustration rig regions, lightweight pseudo mesh deformation, hair/body follow-through, and eye/mouth deformation.
- Stream state machine for idle, preparing, live, reconnecting, stopping, and failed states.
- Mock live engine for UI development without native capture.
- Browser-preview Studio UI:
  - source stack
  - source visibility and lock controls
  - transform sliders
  - program preview
  - mic/lip-sync meter
  - mic effect and monitor controls
  - expression buttons
  - YouTube Live/Twitch/Custom RTMP setup
  - Go Live preflight banner with blocking reasons and operator recommendations
  - readiness panel with blocking errors and warnings
  - stream diagnostics panel with endpoint/quality/telemetry/history/quality-advisor/last-session checks, suggested-quality apply, JSON report export, and support bundle export
  - active stream quality incident panel with operator recommendations
  - stream session event timeline for live support diagnostics
  - quality profiles
  - chat reader panel with test comments, platform adapter ingest, safety controls, speech settings, stream-stop chat auto-disconnect/readout silence, and queued/recent comment privacy reset
  - Go Live, Stop, Reconnect mock controls
- React Native mobile readiness panel, Go Live preflight banner, and invalid-profile blocking.
- React Native mobile stream diagnostics panel using the shared diagnostics model, including health history, quality advisor, last-session summary, sanitized report sharing, and support bundle sharing.
- Web and React Native diagnostics show last-session native runtime evidence for post-stream support triage.
- Web and React Native validation evidence panels show latest-run monitor-hold and native runtime evidence, including native overlay applied/skipped counts and live render-graph reload/rejected-update counters for subtitle/text changes, so release-candidate records can prove the stable private stream, native publisher/compositor path, and in-flight caption/text overlay updates.
- Web and React Native validation evidence panels show latest-run mic FX/headphone monitor route, native self-monitor write/drop proof, and chat readout evidence so release-candidate records can prove voice monitoring and spoken chat behavior.
- Web and React Native validation evidence panels show latest-run platform dashboard and same-run platform ingest evidence so release-candidate records can prove destination-side ingest health.
- Web and React Native validation evidence panels show run/evidence fingerprints for support-bundle audit correlation.
- React Native mobile active stream quality incident panel with operator recommendations.
- React Native mobile stream session event timeline using the shared diagnostics model.
- React Native mobile chat reader panel with test comments, platform adapter ingest, safety controls, and speech settings.
- React Native and web studio previews can position a transparent chat overlay above game/screen capture, and native iOS/Android render graphs receive the same runtime chat payload for RTMP/RTMPS output.
- iOS ReplayKit Broadcast Upload Extension target registered in the Xcode project.
- iOS app-side Broadcast Picker startup bridge with App Group configuration handoff.
- iOS commercial bundle identifiers and App Group alignment for the host app and ReplayKit Broadcast Upload Extension.
- iOS Broadcast Upload Extension runtime state reporting through App Group storage.
- iOS host app runtime telemetry polling from the Broadcast Upload Extension, feeding bitrate, FPS, drops, reconnect attempts, errors, composition status, and still-image asset loaded/missing/decoded/composited pixel counts plus App Group-copied still-image render proof into the React Native snapshot.
- iOS Broadcast Upload Extension VideoToolbox H.264 encoder.
- iOS Broadcast Upload Extension AudioToolbox AAC encoder with app/mic PCM mixing.
- iOS Broadcast Upload Extension microphone DSP applies the shared mic effects profile to ReplayKit microphone PCM before AAC mixing, including input gain, noise gate, compression, bright/robot presets, soft limiting, and runtime processing evidence.
- Shared Audio Peak Guard diagnostics use native mic limiter counters plus retained audio-meter peak/clipping evidence to warn or fail readiness before a public stream starts.
- iOS Broadcast Upload Extension RTMP/RTMPS publisher foundation with H.264/AAC packetization.
- iOS RTMP publisher reconnect backoff state with bounded retry attempts.
- iOS ReplayKit Broadcast Upload Extension compositor for PNGTuber still-image/fallback, text, chat, image, and solid overlays on encoded frames, with App Group runtime composition status, `ios-replaykit-coregraphics` composited-frame/drop/failure counters, and still-image asset load/miss/decode/composited pixel evidence, including separate App Group-copied still-image loaded/decoded/composited proof required by v41+ release evidence.
- iOS native-compositor preflight diagnostics warn when PNGTuber/image overlays use missing, host-sandbox file-backed, relative, content, http(s), data, or otherwise unsupported still-image asset URIs that the Broadcast Upload Extension cannot reliably load.
- iOS mobile still-image asset picker/preparation copies selected PNGTuber/image source files into the shared App Group container and stores a ReplayKit-extension-readable `file://` URI in the scene.
- Android MediaProjection service skeleton.
- Android `LiveCasterNative` React Native module registered in the host app.
- Android MediaProjection consent flow from React Native.
- Android foreground streaming service with `mediaProjection|microphone` service type.
- Android RTMP/RTMPS publishing path through RootEncoder, using screen capture and microphone input.
- Android first-party `MediaCodecList` / `MediaCodec.configure` probe for requested H.264 surface-input and AAC encoder settings, surfaced separately from the active RootEncoder publisher backend in native runtime, validation-run manifest, and support-bundle evidence.
- Android selectable direct MediaCodec RTMP path that captures MediaProjection frames through `ImageReader`, composites screen plus PNGTuber/text/caption/chat/image/solid render-graph overlays onto the first-party H.264 input surface, records runtime compositor backend/composited-frame/drop/failure counters for release evidence, captures microphone PCM through `AudioRecord`, applies the existing mic FX/monitor path before AAC encoding, and sends Annex B-validated H.264 plus AAC output through `RtmpClient` without using RootEncoder `GenericStream`; RootEncoder remains the default compatibility path until the direct path is physically validated.
- Android publish endpoint assembly from the app's server URL and stream key fields.
- Android native GL/Canvas overlay compositors for PNGTuber still-image, text, chat, image, and solid scene sources on top of MediaProjection capture, with applied/pending overlay status, still-image asset load/miss/decode/composited pixel evidence, and Android direct MediaCodec Canvas runtime frame proof in stream health/native runtime telemetry.
- Android mobile still-image asset picker/preparation copies selected content/file/path PNGTuber/image source assets into app-internal storage and stores a stable `file://` URI for native GL overlay rendering.
- Android microphone PCM effect path through RootEncoder `CustomAudioEffect`.
- Android and iOS native runtime telemetry surfaces broadcast mixer gain/mute state and processed/limited mic sample counters for Audio Peak Guard evidence.
- Shared Audio Silence Guard diagnostics warn during live/reconnecting sessions when the mic channel is in the broadcast mix but current retained mic-activity samples are missing, silent, or very low.
- Android headphone-only mic monitor playback through `AudioTrack`, with monitor write/drop counters and AudioTrack-buffer latency estimates surfaced in native runtime telemetry.
- Android `LiveCasterNative.getAudioRoute()` reports current output route and headphone connection state for monitor-safety preflight.
- iOS Broadcast Upload Extension headphone-gated self-monitor playback for processed ReplayKit microphone PCM with runtime route, write/drop, and AVAudioSession latency evidence.
- Android microphone/notification runtime permission preflight before MediaProjection launch.
- Android release signing fail-closed configuration using `MLC_RELEASE_*` keystore inputs instead of debug signing for release artifacts.
- Store-release environment verifier for iOS and Android that checks required Apple team/profile/App Store Connect API-key and Android release keystore inputs before production archive/export/bundle tasks, without logging secret values and while rejecting symlinked signing material or real paths stored inside the repository.
- Distribution artifact manifest tooling for generated Android `.aab` and iOS `.ipa` files, including byte/SHA-256 recording, ZIP entry proof, git commit/dirty-state provenance, minimum-size and ZIP-container validation, AAB/IPA required-entry validation, verification, and optional release-candidate/report artifact capture when the manifest exists.
- Platform dashboard evidence manifest tooling for YouTube/Twitch dashboard screenshots and status JSON files, including byte/SHA-256 recording, PNG dimension recording, screenshot captured-at recording, git commit/dirty-state provenance, status JSON checked-at/state/channel identity summaries, screenshot/status timestamp skew rejection, placeholder identity rejection, placeholder-size screenshot rejection, verification, and optional release-candidate/report artifact capture when the manifest exists.
- Store submission draft/checklist tooling for App Store / Play Console metadata JSON, human review Markdown, and iOS/Android store screenshots, including metadata/review generation from real-device screenshots or canonical workspace UI evidence, screenshot source tracking, final-submission real-device screenshot device/OS label gating, required listing/privacy/data-safety checks, byte/SHA-256 recording, git commit/dirty-state provenance, secret-like text plus personal contact/protocol-less private-link detection, verification, and optional release-candidate/report artifact capture when the checklist exists.
- Store submission approval gate that rejects development-only dirty-worktree, commit-mismatch, or warning-approved RC reports, then verifies a saved release-candidate report, Android `.aab` / iOS `.ipa` distribution evidence, fresh clean same-commit store-release orchestration report evidence, final store-submission checklist, real-device screenshot source labels, screenshot capture freshness, and complete capture of metadata/review/screenshot artifacts in the RC report before App Store / Play Console approval.
- Commercial release gate blocks approval when the current YouTube/Twitch publishing-status snapshot in the support bundle is missing, invalid, or stale, even when retained physical validation evidence is otherwise complete.
- Release evidence package tooling that copies a passed clean RC report, support bundle, dedicated UI evidence JSON from either skipped or in-process browser UI gates, generated artifacts, distribution manifests, platform dashboard evidence, store-submission evidence, physical-device preflight evidence, and optional store-release orchestration reports into a standalone hash-verified archive directory for commercial handoff and store-submission audit, rejects development-only dirty-worktree, commit-mismatch, or warning-approved RC reports, re-runs the archived support bundle's commercial release gate at the RC report timestamp with zero accepted warnings, fails closed when package generatedAt timing, package/report/UI full git commit and dirty-state provenance, commercial distribution/dashboard/store-submission artifact groups, manifest-referenced payloads, unmanifested package files, packaged AAB/IPA ZIP content and required entries, packaged dashboard PNG/status JSON content and screenshot/status timestamp integrity/freshness, packaged real-device store-submission screenshot PNG content/metadata/freshness/build/physical-device label alignment, packaged store-submission metadata schema/reference integrity, non-empty review Markdown, packaged UI evidence schema/freshness/text/screenshot metadata, packaged physical-device preflight metadata, or packaged store-release distribution evidence are missing or invalid, and scans package-level text evidence for unredacted OAuth tokens, stream keys, client secrets, device codes, Authorization headers, email addresses, phone numbers, invite links, and protocol-less private links.
- Store release orchestration command that chains platform release-environment checks, Android App Bundle generation, iOS archive/export, and distribution artifact manifest creation, with dry-run and platform-scoped modes plus optional machine-readable `store-release-orchestration` reports for git commit/dirty-state provenance, step outcomes, and distribution manifest SHA-256 audit evidence; saved store-release reports revalidate the referenced distribution manifest schema, canonical artifact paths, hashes, and required AAB/IPA ZIP entries before release approval, and release-candidate verification now requires and revalidates distribution, fresh dashboard, final store-submission, and fresh full non-skipped store-release handoff evidence before approving RC runs that include store-submission evidence.
- Release configuration verification fails when production Android/iOS build-target source files contain unresolved `TODO`, `FIXME`, or `Not implemented` markers, keeping native release code from regressing into placeholder implementations.
- Production native implementation files are tracked only in the build-target `android/` and `ios/` projects; release configuration verification rejects stale duplicate scaffold sources under `native/` to avoid release-readiness misclassification.
- Android build-type network policy keeps cleartext traffic enabled only for debug development and disables it for release artifacts.
- Native store version audit keeps Android application metadata, iOS host app versioning, and ReplayKit Broadcast Upload Extension versioning aligned before release approval.
- Native release-configuration audit script covering Android release signing, release cleartext-traffic blocking, Android/iOS store version alignment, streaming permissions, OAuth callback schemes, iOS usage descriptions, iOS privacy manifest packaging, unresolved implementation markers in production native sources, and ReplayKit Broadcast Upload Extension bundle identifiers, Debug/Release entitlements, extension-only settings, and matching App Group setup.
- Android Keystore-backed encrypted mobile profile storage for stream keys.
- Mobile stream key clearing by overwriting the secure native profile with a cleared key.
- Durable mobile-side scene collection persistence through Android SharedPreferences and iOS atomic Application Support JSON storage.
- Android TextToSpeech native module for chat read-aloud.
- iOS Keychain-backed mobile profile storage exposed to React Native.
- iOS AVSpeechSynthesizer native module for chat read-aloud.
- iOS `LiveCasterNative.getAudioRoute()` reports `AVAudioSession` output route and headphone connection state for monitor-safety preflight.
- Durable mobile-side profile persistence through the secure native store.
- React Native bare app scaffold generated from React Native 0.85.3.
- Standard `ios/` and `android/` projects for the MobileLiveCaster host app.
- React Native mobile Studio UI that reuses `src/domain` and the mock stream engine.
- React Native mobile source controls can pick or prepare PNGTuber/image still-image assets and VRM/GLB model files for native compositor/loader storage before streaming.
- GitHub Actions `test` status check for PRs and `main` pushes, covering repository automation safety, native release-configuration audit, unit tests, web/RN typecheck, web build, web bundle-size verification, iOS/Android React Native JavaScript bundling, and Android native debug APK build; separate macOS `ios-native` workflow builds the iOS simulator app through Xcode and writes native verification evidence.
- Metro bundle verification for both iOS and Android.
- Homebrew React Native environment installed: JDK 17, Watchman, CocoaPods, Android command line tools.
- Android SDK packages installed: `platforms;android-36`, `build-tools;36.0.0`, `ndk;27.1.12297006`, `platform-tools`.
- Android `assembleDebug` build succeeds.
- Android App Bundle release task is exposed through `npm run android:bundleRelease` and guarded by the store-release environment verifier.
- iOS `pod install` succeeds and generated `MobileLiveCaster.xcworkspace`.
- iOS Simulator Debug build succeeds through `npm run verify:ios-native`, with `.artifacts/ios-native-verification.json` retaining host app bundle evidence, embedded ReplayKit Broadcast Upload Extension evidence, standalone extension build-product evidence, Info.plist/executable hashes, Xcode version, timing, and derived-data path evidence.
- iOS production archive/export helper scripts generate App Store Connect export options from environment variables, require explicit provisioning profiles for both the host app and ReplayKit Broadcast Upload Extension, archive with Apple Distribution signing, and are covered by native release-configuration audit checks.

## Not Implemented Yet

- iOS end-to-end physical device validation against a real RTMP/RTMPS endpoint.
- iOS YouTube Live/Twitch RTMP ingest validation with real stream keys.
- Apple Developer account-backed iOS production archive/export execution with real provisioning profiles and App Store Connect credentials.
- Android end-to-end physical device validation against a real RTMP/RTMPS endpoint.
- Android physical-device tuning for mic monitor latency and Bluetooth route behavior using the retained in-app latency evidence.
- iOS physical-device tuning for mic monitor latency and Bluetooth route behavior using the retained in-app latency evidence.
- iOS physical-device execution of the v51 App Group still-image render-proof run against real devices and real ingest endpoints, including non-zero `ios-replaykit-coregraphics` composited-frame proof and zero composition failures.
- Physical-device validation of the selectable Android direct MediaCodec compositor/publisher path against real RTMP/RTMPS endpoints, including non-zero `android-canvas-mediacodec` composited-frame proof and zero composition failures, before making it the commercial default.
- Native Live2D Cubism SDK rendering integration and physical-device validation. Shared Cubism `model3.json` package validation, local URI checks, packaged-reference safety checks, texture format readiness checks, expression/motion coverage checks, and duplicate-reference warnings are now in place as renderer prerequisites.
- Native VRM/VRoid 3D renderer integration, actual humanoid bone/expression rendering from the delivered pose telemetry, and physical-device validation. Native `.vrm`/`.glb` model selection, storage, GLB/VRM header-load telemetry, VRM version telemetry, unsupported required-extension and self-contained binary-buffer checks, humanoid-bone counts, expression/blendshape counts, mesh/skinned-mesh/triangle-mode/POSITION/NORMAL/TEXCOORD_0 vertex/index/bounds/skinning-attribute/skin-joint/morph/material/texture/image-mime/transparent-material renderability and renderer-compatibility counts, mobile model-size/mesh/material/texture budget warnings, and pose-to-model compatibility telemetry are now in place as renderer prerequisites.
- IRIAM-style high-fidelity single-illustration auto-rigging beyond the current image-aspect-aware geometry plus alpha-mask foreground, browser and native iOS/Android dark-pixel eye/mouth feature extraction, browser FaceDetector handoff, semantic face-landmark lightweight still-image 2.5D deformation, and face-part/pseudo-depth/semantic-segment/eye-mouth-band quality scoring, including production semantic image-segmentation-based face/body part inference, true mesh/depth rigging, and physical-device tuning against native-camera tracking.
- Twitch stream key reset/rotation through API. Twitch currently exposes stream key retrieval through Helix, not a public reset endpoint.

## Local Environment Notes

- Xcode is available.
- JDK 17 is installed through Homebrew.
- CocoaPods is installed through Homebrew.
- Watchman is installed through Homebrew.
- Android Studio already exists in `/Applications/Android Studio.app`.
- Android command line tools are installed through Homebrew.

## Next Slice

1. Run `npm run verify:physical-devices -- --report-json .artifacts/physical-device-preflight.json`, then run iOS and Android on physical devices against a private RTMPS endpoint and tune bitrate/fps/audio sync behavior.
2. Validate YouTube Live and Twitch ingest with real stream keys on both platforms.
3. Validate iOS native compositor output on a physical device and tune frame latency after private endpoint validation.
4. Validate YouTube broadcast status refresh and lifecycle transitions with a real account and active ingest stream.
5. Tune app-level recovery thresholds against measured physical-device ingest behavior.
